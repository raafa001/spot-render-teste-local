kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30443
        hostPort: 443
      - containerPort: 30080
        hostPort: 80
    extrahosts:
      - hostname: host.docker.internal
        ip: 172.17.0.1
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "spot-render=true"
    labels:
      spot-render: "true"
